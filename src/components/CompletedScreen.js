import React from 'react';
import { View, Text } from 'react-native';

const CompletedScreen = () => (
  <View>
    <Text style={{fontWeight:'bold'}}>Completed Screen</Text>
  </View>
);

export default CompletedScreen;
